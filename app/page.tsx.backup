import { redirect } from "next/navigation";

export default function RootPage() {
  // Check authentication on server side (for initial load)
  // In a real app, you'd verify the token here
  // For now, always redirect to login - the dashboard will handle auth check
  redirect("/login");
}
